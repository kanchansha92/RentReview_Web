# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist

.env
dist-ssr
*.local

# Generated by scripts/generate-sitemap.js as a `prebuild` step (see
# package.json's "build" script) -reflects live property data at build time,
# so it doesn't belong in version control. `npm run build` (or `npm run
# generate-sitemap`) regenerates it locally; don't hand-edit it.
public/sitemap.xml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
